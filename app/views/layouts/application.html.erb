<!DOCTYPE html>
<html>
  <head>
    <title>Bookers</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark py-4 mb-4">
        <%= link_to "Bookers", root_path, class: 'font-weight-bold text-light' %>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#drop">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="drop">
          <ul class="navbar-nav ml-auto">
            <% if user_signed_in? %>
              <li>
                <%= link_to "Home", user_path(current_user), class: 'fas fa-home mr-3 text-light' %>
              </li>
              <li>
                <%= link_to "Users", users_path, class: 'fas fa-users mr-3 text-light' %>
              </li>
              <li>
                <%= link_to "Books", books_path, class: 'fas fa-book-open mr-3 text-light' %>
              </li>
              <li>
                <%= link_to 'logout', destroy_user_session_path, method: :delete, class: 'fas fa-sign-out-alt mr-3 text-light' %>
              </li>
            <% else %>
              <li>
                <%= link_to 'Home', root_path, class: 'fas fa-home mr-3 text-light' %>
              </li>
              <li>
                <%= link_to 'About', homes_about_path, class: 'fas fa-link mr-3 text-light' %>
              </li>
              <li>
                <%= link_to 'sign up', new_user_registration_path, class: 'fas fa-user-plus mr-3 text-light' %>
              </li>
              <li>
                <%= link_to 'login', new_user_session_path, class: 'fas fa-sign-in-alt mr-3 text-light' %>
              </li>
            <% end %>
          </ul>
        </div>
      </nav>
    </header>
    <main>
      <%= yield %>
    </main>
    <footer>
      <div class='container mt-5'>
        <div class='row'>
          <div class='mx-auto'>
            <p>CopyRight Infratop.inc</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
